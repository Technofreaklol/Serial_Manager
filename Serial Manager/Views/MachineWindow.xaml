<Window x:Class="SerialManager.Views.MachineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Maschinenverwaltung"
        Height="500"
        Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Maschine">

            <Grid Margin="10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Name:"
                           VerticalAlignment="Center"/>

                <TextBox x:Name="txtMachine"
                         Grid.Column="1"
                         Margin="5"/>

                <StackPanel Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10,0,0">

                    <Button Content="Speichern"
                            Width="90"
                            Margin="5"
                            Click="Save_Click"/>

                    <Button Content="Löschen"
                            Width="90"
                            Margin="5"
                            Click="Delete_Click"/>

                </StackPanel>

            </Grid>

        </GroupBox>

        <DataGrid x:Name="dgMachines"
                  Grid.Row="1"
                  Margin="0,15,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="dgMachines_SelectionChanged">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Maschine"
                                    Binding="{Binding Name}"
                                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>