<Window x:Class="SerialManager.Views.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update verfügbar"
        Height="440"
        Width="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblHeadline"
                   FontSize="15"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Text="Eine neue Version ist verfügbar"/>

        <TextBlock x:Name="lblVersions"
                   Grid.Row="1"
                   Margin="0,6,0,10"
                   TextWrapping="Wrap"/>

        <GroupBox Grid.Row="2"
                  Header="Was ist neu">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="lblReleaseNotes"
                           Margin="6"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </GroupBox>

        <StackPanel x:Name="panelProgress"
                    Grid.Row="3"
                    Margin="0,12,0,0"
                    Visibility="Collapsed">

            <TextBlock x:Name="lblProgressStatus"
                       Margin="0,0,0,4"
                       Text="Update wird heruntergeladen..."/>

            <ProgressBar x:Name="progressBar"
                         Height="18"
                         Minimum="0"
                         Maximum="100"/>

        </StackPanel>

        <StackPanel x:Name="panelButtons"
                    Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <Button x:Name="btnSkip"
                    Content="Version überspringen"
                    Width="160"
                    Click="BtnSkip_Click"/>

            <Button x:Name="btnLater"
                    Content="Später erinnern"
                    Width="130"
                    Margin="10,0,0,0"
                    Click="BtnLater_Click"/>

            <Button x:Name="btnUpdate"
                    Content="Jetzt aktualisieren"
                    Width="150"
                    Margin="10,0,0,0"
                    IsDefault="True"
                    Click="BtnUpdate_Click"/>

        </StackPanel>

    </Grid>

</Window>
