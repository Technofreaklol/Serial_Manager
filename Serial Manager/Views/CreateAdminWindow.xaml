<Window x:Class="SerialManager.Views.CreateAdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SerialManager.Views"
        mc:Ignorable="d"
        Title="Administrator-Konto erstellen"
        Height="340" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2"
                   Text="Beim ersten Start wird ein Administrator-Konto benötigt."
                   TextWrapping="Wrap"
                   FontWeight="SemiBold"
                   Margin="0,0,0,18"/>

        <TextBlock Grid.Row="1"
                   Text="Name:"
                   VerticalAlignment="Center"/>
        <TextBox x:Name="txtName"
                 Grid.Row="1" Grid.Column="1"
                 Height="28" Margin="0,5"/>

        <TextBlock Grid.Row="2"
                   Text="Benutzername:"
                   VerticalAlignment="Center"/>
        <TextBox x:Name="txtUsername"
                 Grid.Row="2" Grid.Column="1"
                 Height="28" Margin="0,5"/>

        <TextBlock Grid.Row="3"
                   Text="Passwort:"
                   VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtPassword"
                     Grid.Row="3" Grid.Column="1"
                     Height="28" Margin="0,5"/>

        <TextBlock Grid.Row="4"
                   Text="Wiederholen:"
                   VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtPassword2"
                     Grid.Row="4" Grid.Column="1"
                     Height="28" Margin="0,5"/>

        <Button Grid.Row="5"
                Grid.ColumnSpan="2"
                Height="35"
                Margin="0,20,0,0"
                Content="Administrator anlegen"
                Click="Create_Click"/>

    </Grid>
</Window>