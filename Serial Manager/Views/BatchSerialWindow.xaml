<Window x:Class="SerialManager.Views.BatchSerialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Batch-Seriennummern"
        Height="380"
        Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Artikel:"
                   VerticalAlignment="Center"/>

        <ComboBox x:Name="cmbArticle"
                  Grid.Column="1"
                  DisplayMemberPath="ArticleNumber"
                  SelectionChanged="Input_Changed"/>

        <TextBlock Grid.Row="1"
                   Text="Maschine:"
                   Margin="0,10,0,0"
                   VerticalAlignment="Center"/>

        <ComboBox x:Name="cmbMachine"
                  Grid.Row="1"
                  Grid.Column="1"
                  Margin="4,10,4,0"
                  DisplayMemberPath="Name"/>

        <TextBlock Grid.Row="2"
                   Text="Anzahl:"
                   Margin="0,10,0,0"
                   VerticalAlignment="Center"/>

        <TextBox x:Name="txtQuantity"
                 Grid.Row="2"
                 Grid.Column="1"
                 Margin="4,10,4,0"
                 Text="100"
                 TextChanged="Input_Changed"/>

        <GroupBox Grid.Row="3"
                  Grid.ColumnSpan="2"
                  Margin="0,20,0,0"
                  Header="Vorschau">

            <StackPanel Margin="10">

                <TextBlock x:Name="txtPreviewStart"/>

                <TextBlock x:Name="txtPreviewEnd"
                           Margin="0,5,0,0"/>

            </StackPanel>

        </GroupBox>

        <StackPanel Grid.Row="5"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

            <Button Width="120"
                    Content="Erzeugen"
                    Click="Generate_Click"/>

            <Button Width="120"
                    Margin="10,0,0,0"
                    Content="Schließen"
                    Click="Close_Click"/>

        </StackPanel>

    </Grid>

</Window>