<Window x:Class="SerialManager.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Passwort ändern" Height="260" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Aktuelles Passwort:" VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtCurrentPassword" Grid.Column="1" Margin="0,4" Height="30"/>

        <TextBlock Grid.Row="1" Text="Neues Passwort:" VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtNewPassword" Grid.Row="1" Grid.Column="1" Margin="0,4" Height="30"/>

        <TextBlock Grid.Row="2" Text="Wiederholen:" VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtNewPassword2" Grid.Row="2" Grid.Column="1" Margin="0,4" Height="30"/>

        <TextBlock x:Name="lblError" Grid.Row="3" Grid.ColumnSpan="2"
                   Foreground="Red" TextWrapping="Wrap" Margin="0,6"/>

        <Button Grid.Row="4" Grid.ColumnSpan="2" Height="35" Margin="0,10,0,0"
                Content="Passwort ändern" Click="Save_Click"/>
    </Grid>
</Window>